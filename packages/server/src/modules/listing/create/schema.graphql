scalar Upload

input CreateListingInput {
  artist: String!
  picture: Upload
  venue: String!
  date: String!
  spotifyId: String!
}

type Mutation {
  createListing(input: CreateListingInput!): String!
}
